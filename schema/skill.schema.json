{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://onehorizon.ai/directory/schema/skill.schema.json",
  "title": "SKILL.md frontmatter",
  "description": "Structural schema for the YAML frontmatter of skills/<name>/SKILL.md. `name` and `description` are the Agent Skills spec fields every compatible runtime reads. Everything directory/website-only lives under `metadata` so it never collides with runtime-read fields.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "description", "metadata"],
  "properties": {
    "name": {
      "type": "string",
      "description": "Kebab-case identifier. Must exactly equal the skill's folder name.",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "description": {
      "type": "string",
      "description": "Runtime-triggering description read by agents deciding whether to invoke this skill. Optimized for triggering, not display copy.",
      "minLength": 1
    },
    "metadata": {
      "type": "object",
      "description": "Directory/website-only fields. Not part of the Agent Skills spec; nested here so runtime tooling that only reads `name`/`description` is unaffected.",
      "additionalProperties": false,
      "required": ["title", "tagline", "category"],
      "properties": {
        "title": {
          "type": "string",
          "description": "Human display title for the listing card, e.g. \"Review Code\".",
          "minLength": 1
        },
        "tagline": {
          "type": "string",
          "description": "Short marketing-style blurb for the card. Written for a human scanning a list, not for agent triggering.",
          "minLength": 1,
          "maxLength": 160
        },
        "category": {
          "type": "string",
          "description": "Must be one of the ids in schema/categories.json.",
          "minLength": 1
        },
        "tags": {
          "type": "array",
          "description": "Optional lowercase-kebab tags. Deduped by the validator.",
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
          }
        },
        "compatibility": {
          "type": "array",
          "description": "Optional list of runtime ids (see schema/runtimes.json) this skill depends on. Omit when the skill works on any Agent-Skills-compatible runtime.",
          "items": {
            "type": "string"
          }
        },
        "license": {
          "type": "string",
          "description": "Defaults to the repo LICENSE when omitted."
        },
        "author": {
          "type": "string"
        },
        "version": {
          "type": "string"
        },
        "sourceRepo": {
          "type": "string"
        },
        "taxonomyId": {
          "type": "string",
          "description": "Reserved for the future mapping to the One Horizon workspace Taxonomy > Skills page. Not wired up yet."
        }
      }
    }
  }
}
